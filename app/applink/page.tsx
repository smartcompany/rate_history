import AppLinkLanding, { createApplinkMetadata } from "./AppLinkLanding";

export const metadata = createApplinkMetadata("/applink");

/**
 * 앱 공유 링크. OG·스토어 랜딩(이전에는 여기서만 스토어 redirect만 해 링크 미리보기가 비었음).
 */
export default AppLinkLanding;
